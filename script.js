document.addEventListener('DOMContentLoaded', () => {const containerWrapper = document.querySelector('.container-wrapper');const timeDisplayTop = document.createElement('div');timeDisplayTop.classList.add('time-display');containerWrapper.insertBefore(timeDisplayTop, containerWrapper.firstChild);const timeDisplayBottom = document.createElement('div');timeDisplayBottom.classList.add('time-display');containerWrapper.appendChild(timeDisplayBottom);const startDate = new Date(2004, 0, 6);function updateSeconds() {const currentDate = new Date();const secondsElapsed = Math.floor((currentDate - startDate) / 1000);const repeatedSeconds = `${secondsElapsed}`.repeat(1000);timeDisplayTop.textContent = repeatedSeconds;timeDisplayBottom.textContent = repeatedSeconds;}updateSeconds();setInterval(updateSeconds, 1000);});